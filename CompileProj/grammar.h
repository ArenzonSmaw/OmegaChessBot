#ifndef GRAMMAR_H
#define GRAMMAR_H

#define TOKEN_MAX_LENGTH 33

#include <stdlib.h>
#include <stdio.h>

typedef enum {

	//TERMINALS
	ID,
	INT_LITERAL,
	FLOAT_LITERAL,
	RAT_LITERAL,
	CHR_LITERAL,
	STR_LITERAL,
	BOOL_LITERAL,

	TYPE,

	UNKNOWN_OPERATOR,
	AND,
	ANDAND,
	OR,
	OROR,
	TILDE_OR,
	EQUALS,
	DBL_EQUALS,
	RIGHT,
	DBL_RIGHT,
	RIGHT_EQUALS,
	LEFT,
	DBL_LEFT,
	LEFT_EQUALS,
	NOT_EQUALS,
	PLUS,
	DBL_PLUS,
	MINUS,
	DBL_MINUS,
	MULT,
	DBL_MULT,
	DIVIDE,
	DBL_DIVIDE,
	MOD,
	DBL_MOD,
	NOT,
	NOTNOT,
	COLON,
	ARROW,

	SEMICOLON,
	COMMA,
	OP_RNDBRACKET,
	CL_RNDBRACKET,
	OP_CRLBRACKET,
	CL_CRLBRACKET,
	OP_SQRBRACKET,
	CL_SQRBRACKET,
	RETURN,
	BREAK,
	PASS,
	LOOP,
	DECLARE,
	USE,

	IF,
	ELSE,
	CHECK,
	EXCEPTION,
	UNDERLINE,
	END_TOKEN,
	TERMINALS_COUNT,

	//NON TERMINALS
	LITERAL,
	PARAMETER,
	PARAM_LIST,
	FACTOR,
	ARG_LIST,
	TERM,
	ARITH_EXPR,
	CMPR_EXPR,
	LOGIC_EXPR,
	EXPRESSION,
	INCREMENTAL,
	STATEMENT,
	STMT_LIST,
	BLOCK,
	PROGRAM,
	S_TAG,

	SYMBOLS_COUNT
} symbol;


typedef struct
{
	symbol symbol;
	int isTerminal;
} item;

typedef struct
{
	item lhs;
	item* rhs;
	int pos, length;
	int rule_num;
} item_set, * items_arr;

item_set* rule(item, item*);

#endif
